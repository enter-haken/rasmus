# backend configuration

currently there is only a `dev` configuration.

## database

    config :backend, :pg_config, 
      hostname: "localhost",
      username: "postgres",
      password: "postgres",
      database: "rasmus"


